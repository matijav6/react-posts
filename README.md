# React app for displaying placeholder posts with comments

#### To run app, just install modules (yarn install) and run it (yarn start)

#### To run app, just run <code>yarn test</code>

#### Approximate time spent: 16h  
